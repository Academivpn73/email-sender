name: Keep Email Active

on:
  schedule:
    - cron: "*/10 * * * *"   # اجرای خودکار هر 10 دقیقه
  workflow_dispatch:          # اجرای دستی هم ممکن است

jobs:
  send_email:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ secrets.EMAIL_USERNAME }}   # ایمیل فرستنده
          password: ${{ secrets.EMAIL_PASSWORD }}   # App Password
          subject: "📩 Ping Email"
          body: "این یک ایمیل تستی برای فعال نگه داشتن حساب است."
          from: KeepAlive <${{ secrets.EMAIL_USERNAME }}>
          to: academi.mah88@gmail.com
